name = "bombergs-relay"
main = "src/index.js"
compatibility_date = "2025-01-01"

[[durable_objects.bindings]]
name = "ROOM"
class_name = "Room"

# Durable Objects need a migration to be created. The SQLite-backed class is
# the one available on the Workers free plan.
[[migrations]]
tag = "v1"
new_sqlite_classes = ["Room"]
